#include<iostream>
using namespace std;
bool is_prime(int num)
{
	for (int i = 2; i < num; i++)
	{
		if (num % i == 0)
			return false;
		else
			return true;
	}
}
int main()
{
	int count = 0;
	int num=2;
	while (count < 200)
	{
		if (is_prime(num)) {
			count++;
			cout << num << " ";
			num++;
		}
		else {
			num++;
		}
		if (count % 10 == 0)
		{
			cout <<endl;
	}
	}
}
